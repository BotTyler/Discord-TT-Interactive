# server {
#     listen 80;
#     server_name _;
#
#     root /usr/share/nginx/html;
#     index index.html;
#
#     # Serve static assets
#     location / {
#         try_files $uri /index.html;
#     }
#
#     # Optional: Improve caching for static files
#     location ~* \.(js|css|png|jpg|jpeg|gif|svg|ico)$ {
#         expires 30d;
#         access_log off;
#     }
# }


server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Serve static files
    location / {
        try_files $uri /index.html;
    }

}

